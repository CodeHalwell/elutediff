"""METLIN SMRT loading, molecule featurization, splits, and graph features.

These modules depend on RDKit (install the ``chem`` extra). They are scaffolded
with the intended public interface; implementation lands in roadmap step 1.
"""
